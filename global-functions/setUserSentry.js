import * as CommonPackages from '../custom-files/CommonPackages';

const setUserSentry = userId => {
  // try {
  //     const Sentry = CommonPackages?.Sentry;
  //     if (userId !== null && userId !== undefined) {
  //         Sentry.setUser({ id: userId });
  //     } else {
  //         Sentry.setUser(null);
  //     }
  // } catch (error) {
  //     console.error(error);
  // }
};

export default setUserSentry;
